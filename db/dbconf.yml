testing:
  driver: postgres
  open: user=transform_chiropratic host=localhost dbname=transform_chiropratic password=supersecret sslmode=disable
development:
  driver: postgres
  open: user=transform_chiropratic host=localhost dbname=transform_chiropratic password=supersecret sslmode=disable
staging:
  driver: postgres
  open: user=template password=template host=localhost dbname=template sslmode=require
production:
  driver: postgres
  pen: user=template password=template host=localhost dbname=template sslmode=require
